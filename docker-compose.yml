services:
    phenix:
        build:
            context: .
            dockerfile: Dockerfile

        container_name: phenix

        volumes:
            - .:/app

        working_dir: /app

        stdin_open: true
        tty: true
